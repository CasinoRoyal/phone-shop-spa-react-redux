import * as R from 'ramda';

export const getPhoneById = (state, id) => R.prop(id, state.phones);

export const getPhones = (state) => {
  const phones = R.map((id) => getPhoneById(state, id), state.phonesPage.ids);
  return phones;
};

export const getPhonesPageLength = (state) => state.phonesPage.length;

export const getCartItemsLength = (state) => state.cart.length;

export const getTotalCartItems = (state) => {
  const totalPrice = R.compose(
    R.sum,
    R.pluck('price'),
    R.map(id => getPhoneById(state, id))
  )(state.cart)

  return totalPrice
};